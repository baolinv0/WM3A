# 联合3A方案内容分析及DOI交底书

## 一、联合3A专利内容逐步分析

### 1. 方案所属技术方向

本方案属于移动终端影像处理与相机自动控制技术领域，具体涉及一种将自动曝光 AE、自动对焦 AF、自动白平衡 AWB 与图像信号处理单元 ISP 的退化恢复能力进行联合建模和联合控制的方法。

该方案关注的不是单一 AE、AF 或 AWB 模块本身，而是前端 3A 拍摄参数与后端 ISP 恢复能力之间的协同关系。核心目标是在低光运动、高动态范围夜景、混合光照视频等复杂场景下，避免某一个 3A 模块的独立最优决策破坏其他模块的输入质量，从而提高最终图像或视频的清晰度、颜色稳定性和整体保真度。

### 2. 现有3A系统的基本工作方式

现有商业相机系统通常采用独立反馈闭环：

- AE 读取亮度统计信息，控制传感器增益 ISO 和曝光时间 T；
- AF 读取对比度、相位差或双像素信息，控制镜头马达或焦点位置；
- AWB 读取颜色统计信息，控制 RAW 图像中的 RGB 增益；
- 语义图、硬件统计引擎、传感器状态等信息可以被各模块共享，但多数情况下只是作为被动状态或加权输入，而不是用于真正的联合约束。

这种架构证明了共享 3A 上下文在工程上可行，但通常只实现了结构协同，并未实现算法意义上的联合优化。各模块会写入自己的状态，但很少主动吸收其他模块的退化状态，也很少根据其他模块的恢复边界约束自身决策。

### 3. 现有方案的主要问题

现有方案的第一个问题是，各模块独立调参会物理污染原始图像信号。以低光运动场景为例，AE 为了提升亮度会延长曝光时间或提高 ISO，但长曝光会造成运动模糊，高 ISO 会放大噪声；当运动位移超过 PDAF 像素对间距或相位检测信号被噪声淹没时，AF 难以判断真实焦点。此时 AE 的亮度目标虽然被满足，但 AF 的输入已经被破坏。

现有方案的第二个问题是，AE 过曝会破坏 AWB 采样。在高对比夜景中，AE 可能为了暗部曝光而牺牲高亮区域，导致光源或反光区域饱和。饱和像素不再携带有效颜色信息，AWB 的颜色比例估计会发生漂移，最终造成全局偏色。

现有方案的第三个问题是，AWB 在混合光照视频中容易产生自不稳定。AWB 色温估计在帧间跳变时，会引起 R/B 增益的大幅调整，造成可见的帧间颜色闪烁。简单平滑 AWB 可以降低跳变，但当真实照明发生变化时又会引入颜色拖影。

现有方案的第四个问题是，前端 3A 模块通常独立定义自己的目标，而没有考虑 ISP 的最大恢复能力。最终图像质量同时由传感器采集、3A 参数、ISP 降噪、去模糊、色彩校正、高光恢复等共同决定。如果 3A 引入的噪声、模糊、过曝或偏色超过 ISP 的可恢复边界，后端算法无法弥补前端损伤。

### 4. 现有改进尝试及不足

现有方案中存在一些改进尝试，例如共享 AE、AF、AWB 当前决策结果，或者将 AF 搜索安排在 AE 锁定之后，再启动 AWB。该类串行化方法可以减少部分冲突，但会降低收敛速度，并且没有定义当模块目标冲突时应由谁牺牲、牺牲多少、依据什么边界牺牲。

还有一些方法尝试让模块之间传递需求，例如 AF 告知 AE 需要更短曝光以避免运动模糊，AWB 告知 AE 需要保留高光颜色样本。但这些方法如果缺少恢复能力边界，就无法判断某种退化是否可由 ISP 后处理恢复，也无法判断哪些区域、哪些语义对象、哪些拍摄模式下允许更大的前端退化。

因此，仅共享信息不足以解决问题。关键在于建立“退化特征”和“ISP恢复能力”之间的对应关系，并将该对应关系反向注入到 3A 参数决策中。

### 5. 本方案的核心发明逻辑

本方案的核心思想可以概括为：

通过预测图像中不同语义区域、不同场景状态和不同拍摄模式下的 ISP 恢复能力，将该恢复能力作为 3A 参数搜索的物理边界，使 AE、AF、AWB 不再分别追求局部最优，而是在 ISP 可恢复范围内联合决定曝光时间、ISO、焦点位置和白平衡增益。

换言之，本方案不是简单地“共享 3A 信息”，而是把 ISP 的最大可修复退化能力前向注入到 AE/AF/AWB 决策中，再通过跨模块退化信号反馈，使各模块在造成不可逆损伤之前调整拍摄参数。

### 6. 方案中的主要模块

本方案主要包括以下模块：

1. **ISP恢复能力预标定模块**：在 Lux、语义类别、温度、帧率、拍摄模式等维度上，标定 ISP 对噪声、失焦、运动模糊、高光溢出和色彩偏移的最大恢复能力。

2. **ISP空间-语义恢复能力调制模块**：根据 RAW 图像、语义图、图像统计量、相机状态和 SNR 等信息，将离线标定的恢复能力转化为像素级或区域级恢复能力图。

3. **物理约束 AE 模块 PCF-AE**：根据亮度目标、AF/AWB 的退化需求和 ISP 恢复能力，对曝光时间和 ISO 进行可靠性引导的修正。

4. **可靠性引导 AF 特征校正模块 RPF-AF**：根据 AE 引起的噪声和运动模糊、AWB 引起的增益变化以及 ISP 对失焦的恢复能力，对双像素或对焦特征进行校正。

5. **因果调制 AWB 模块 CM-AWB**：根据 AE/AF 引入的退化和 ISP 色彩恢复能力，过滤失效颜色样本，并结合时间历史实现稳定白平衡。

### 7. 可保护的核心创新点

本方案的核心可保护点主要包括：

- 基于 ISP 恢复能力边界确定 3A 参数可调范围；
- 基于语义区域和退化类型建立 ISP 可恢复能力图；
- 在 AE 中引入 AF/AWB 退化需求和 ISP 恢复边界进行曝光修正；
- 在 AF 中引入 AE/AWB 退化状态进行可靠性引导的对焦特征校正；
- 在 AWB 中基于 AE/AF 退化状态和 ISP 色彩恢复能力构建有效颜色证据池；
- 将噪声、模糊、过曝、偏色等退化特征作为跨模块反馈信号，实现联合 3A 控制。

---

## 二、联合3A方案DOI交底书

### 1. 发明背景介绍

#### a. 发明的技术领域

本发明属于移动影像和智能终端相机控制技术领域，具体涉及一种结合自动曝光 AE、自动对焦 AF、自动白平衡 AWB 以及图像信号处理单元 ISP 恢复能力，进行拍摄参数联合控制的方法、装置、电子设备及存储介质。

更具体地，本发明涉及一种基于图像退化特征和 ISP 可恢复能力边界的联合 3A 控制方法，通过对噪声、模糊、过曝、偏色等退化特征进行建模，并结合不同语义区域、场景状态和拍摄模式下的 ISP 恢复能力，动态确定 ISO、曝光时间、镜头位置和白平衡参数的可调范围，从而在复杂拍摄场景下提升图像清晰度、颜色准确性和视频稳定性。

#### b. 现有技术的说明

目前，智能手机和相机系统普遍采用 AE、AF、AWB 三个独立模块进行拍摄控制。其中，AE 根据图像亮度统计信息调整曝光时间和传感器增益 ISO；AF 根据对比度、相位差、双像素信息或学习特征调整镜头位置；AWB 根据颜色统计信息调整 RAW 图像或 ISP 中的 RGB 增益。

在传统商业 3A 架构中，硬件统计引擎和传感器可以向多个模块提供共享数据，例如亮度统计、对比度统计、颜色统计、语义图、传感器温度、帧率和拍摄模式等。部分方案还通过规则调度器控制 AE、AF、AWB 的运行顺序，例如先等待 AE 收敛，再触发 AF 搜索，随后启用 AWB。

这种方案在普通场景下能够完成基本拍摄控制，但在低光运动、高对比夜景、混合光照视频等复杂场景中，各模块之间存在明显的物理耦合。一个模块独立优化自身目标时，可能会改变其他模块依赖的输入信号，使其他模块读取到被污染的统计信息。

例如，在低光运动场景中，AE 为了提高画面亮度会延长曝光时间或提高 ISO。曝光时间过长会导致运动模糊，ISO 过高会放大噪声。当相位差特征被运动模糊和噪声破坏后，AF 难以从图像中提取可靠焦点信息，最终出现对焦失败。

又例如，在极端高对比夜景中，AE 为了提升暗部亮度可能导致高亮区域饱和。饱和像素不再携带有效颜色信息，AWB 在这些区域进行采样时会得到错误的颜色比例，从而导致全局偏色。

再例如，在混合光照视频中，AWB 会在不同光源之间反复切换色温估计，引起 R/B 增益在帧间跳变，造成明显的颜色闪烁。若简单地对 AWB 增益进行平滑，又可能在真实照明变化时造成颜色滞后和偏移。

#### c. 现有技术问题或要改善的地方

现有技术在联合 3A 控制中主要存在以下两类问题：

1、独立拍摄参数调节会物理污染 RAW 图像信号，其他模块会将被污染的信息作为干净输入继续处理，导致级联错误并生成质量较差的图像。

现有 3A 控制方法通常依赖独立反馈闭环提取孤立的图像特征，并默认各模块之间不存在物理资源冲突。然而在实际拍摄过程中，当一个模块优化自身目标时，会直接在 RAW 信号中引入退化特征，例如噪声、运动模糊、失焦模糊、颜色偏移或高光饱和。后续模块如果仍将这些被污染的信号作为正常输入，则无法感知跨模块干扰，容易在像素层面掩盖真实退化来源。

例如，在低光运动场景中，AE 为了提升亮度会延长曝光时间或提高 ISO。延长曝光时间会引入运动模糊，提高 ISO 会引入噪声，二者均会破坏 AF 所依赖的相位差、对比度或双像素特征，并影响 AWB 所依赖的颜色采样。由于 AE 同时控制曝光时间和 ISO，其本身具有在亮度、噪声和运动模糊之间进行折中的调节空间。如果 AF 需要更高锐度，AE 应当缩短曝光时间并在可接受范围内提高 ISO；如果 AWB 需要保留更多有效颜色像素，AE 应当限制高光饱和和过高 ISO。相比在后端强行补偿，由源头对 AE 施加约束更有效。

又例如，现有架构缺少全局退化特征广播机制。AE、AF、AWB 在运行过程中都会产生不可避免的物理副作用，例如某次曝光对应的噪声程度、运动模糊程度、失焦程度和偏色程度。传统架构通常不会将这些退化特征作为全局状态广播给其他模块，导致接收模块误把已经被光学或传感器过程污染的数据理解为真实场景信息。

2、前端 3A 模块独立定义自身目标，忽略 ISP 单元的最大恢复能力，容易产生超过硬件和算法恢复上限的退化特征，导致纹理细节永久丢失。

最终图像质量由前端传感器采集和后端 ISP 退化恢复共同决定。ISP 可以对噪声、模糊、过曝和偏色进行一定程度的恢复，但这种恢复能力具有明确上限，并且在不同语义区域、不同照度、不同温度和不同拍摄模式下并不相同。现有 3A 方法通常只考虑当前模块的目标是否满足，例如 AE 是否达到亮度目标、AF 是否达到焦点目标、AWB 是否达到颜色统计目标，而没有判断这些目标所引入的退化是否仍处于 ISP 可恢复范围内。

例如，低光场景下 AE 可能采用折中曝光参数：一方面提高 ISO 以保证亮度，另一方面延长曝光时间以压低增益需求。但若该参数组合产生的噪声和运动模糊超过 ISP 最大恢复能力，则后端降噪和去模糊无法恢复纹理结构，最终造成细节丢失。此时继续优化 AE 的亮度目标并不能提升最终图像质量，反而会消耗或超过后端可恢复资源。

因此，现有技术的根本不足不是单个 AE、AF 或 AWB 模块性能不够，而是缺少一种能够同时感知跨模块退化、广播退化特征，并依据 ISP 恢复能力边界约束前端 3A 参数的联合控制机制。

#### d. 现有问题的解决方案以及本发明的目的

针对上述问题，本发明提出一种基于 ISP 恢复能力边界的联合 3A 控制方法。该方法通过在 AE、AF、AWB 参数决策前，先确定当前图像及其语义区域对应的退化特征和 ISP 恢复能力，再基于退化特征与恢复能力之间的差值确定可靠性度量和参数可调范围，最后在该可调范围内联合确定 ISO、曝光时间、焦点位置和白平衡参数。

本发明的目的在于解决现有 3A 模块独立优化导致的跨模块干扰问题。具体而言，本发明旨在：

1. 将 ISP 对噪声、模糊、过曝和偏色的最大恢复能力显式引入 3A 控制；

2. 根据语义区域、照度、温度、帧率和拍摄模式动态生成 ISP 恢复能力边界；

3. 使 AE 在追求亮度目标时同时考虑 AF 的对焦可行性和 AWB 的颜色采样有效性；

4. 使 AF 在提取对焦特征时抑制由 AE 噪声、AE 运动模糊和 AWB 增益变化引入的不可靠特征；

5. 使 AWB 在估计色温时过滤由过曝、噪声和失焦导致的无效颜色样本，并利用时间信息实现稳定的白平衡估计；

6. 在保证图像质量的同时减少不可恢复退化，提升低光运动、高对比夜景和混合光照视频场景下的成像稳定性。

### 2. 发明的详细说明

为了更好地解决现有 3A 模块相互干扰的问题，本发明不是简单地将 AE、AF、AWB 的输出结果共享，而是首先建立 ISP 恢复能力边界，再将该边界作为联合 3A 控制的物理约束。与单独优化亮度、焦点或色温不同，本发明将最终图像质量理解为前端采集退化和后端恢复能力共同作用的结果。

#### a. 发明的构成

如图 1 所示，本发明的整体方案包括输入状态获取模块、ISP 恢复能力边界建模模块、3A 联合策略模块和拍摄参数输出模块。

输入状态可以包括 RAW 图像、RGB 图像、语义图、亮度直方图、图像统计信息、ISO、曝光时间、焦点位置、白平衡增益、设备温度、拍摄帧率、拍摄模式、IMU 信息和环境光传感器 ALS 信息。

输出参数可以包括 ISO、曝光时间、镜头位置或焦距、白平衡参数、HDR/MF 标志，以及 AE、AF、AWB 之间互相传递的退化反馈信号。

具体地，本发明可包括以下四个主要部分：

1. 基于 ISP 恢复能力的场景自适应边界约束模块；

2. 基于恢复能力边界的物理约束 AE 模块；

3. 基于可靠性引导的 AF 特征校正模块；

4. 基于因果调制的 AWB 模块。

#### 1. 基于ISP恢复能力的场景自适应边界约束模块

该模块用于确定当前图像中不同区域、不同退化类型对应的最大可恢复能力。其核心作用是将后端 ISP 能够恢复的退化范围，转化为前端 3A 模块可直接使用的参数约束。

该模块包括 ISP 恢复能力预标定模块和 ISP 空间-语义恢复能力调制模块。

##### Innovation 1.1: ISP恢复能力预标定模块

基于图像照度 Lux、语义类别 Semantic、设备温度 Temperature、拍摄帧率 FPS 和拍摄模式 Mode，构建五维输入状态空间，并在该状态空间中标定 ISP 对不同退化类型的最大恢复能力。

具体包括以下四类标定：

1）语义感知噪声预算标定：

在固定照度下逐步提高 ISO，对不同语义区域进行 ISP 降噪处理，并基于视觉噪声 VN 和锐度保留程度判断对应区域的最大可恢复噪声。对于人脸区域，要求皮肤纹理保留较高；对于纹理区域，要求纹理细节保留；对于平坦区域，可以允许更强降噪。由此得到不同语义类别对应的噪声恢复能力上限。

2）失焦预算标定：

通过轴向移动镜头或测试靶，构造不同程度的失焦模糊，经 ISP 去模糊后检测 MTF50 和过冲比例。当 MTF50 下降或边缘过冲超过阈值时，认为该失焦程度超过 ISP 可恢复范围。由此得到失焦模糊对应的恢复能力上限。

3）运动模糊预算标定：

通过机械臂或运动平台构造不同方向、不同速度的运动模糊，并结合温度状态和多帧降噪 MFNR 能力，评估 ISP 对运动模糊和噪声的联合恢复上限。由于高温会降低多帧处理能力，本发明将温度作为动态状态引入运动模糊恢复能力标定。

4）高光与色彩预算标定：

通过逐步增加过曝量，评估 tone mapping 后的亮度保留、对比度是否反转、色调是否断裂，以及 R/G、B/G 比例是否仍处于可靠范围内。若过曝导致颜色采样失效或 AWB 无法获得有效颜色统计，则认为该区域超过高光和色彩恢复边界。

通过上述标定，可以得到与噪声、失焦、运动模糊、高光溢出和色彩偏移对应的 ISP 最大恢复能力。

##### Innovation 1.2: ISP空间-语义恢复能力调制模块

仅有离线标定的恢复能力不足以描述当前图像的局部差异。因此，本发明进一步将离线标定结果映射为像素级或区域级恢复能力图。

具体步骤如下：

Step1：根据输入 RAW 图像提取视觉特征和状态特征。通过感知网络获得空间语义掩码，例如人脸、天空、纹理区域和平坦区域；同时获得物理退化图，例如 SNR 图、局部对比度图和高光溢出图。

Step2：根据当前照度、语义类别、温度、帧率和拍摄模式查询离线能力 LUT，得到噪声、失焦、运动模糊、高光和色彩偏移的基础恢复预算。

Step3：将上述恢复预算输入多层感知机 MLP，编码为连续恢复能力嵌入向量。

Step4：将恢复能力嵌入向量广播到空间维度，并结合语义掩码进行空间调制，使不同语义区域具有不同恢复能力。例如，人脸区域对噪声和色彩偏移更敏感，纹理区域对过度降噪更敏感，平坦区域可以接受更强降噪。

Step5：基于 SNR 图和局部对比度图计算动态衰减因子。当某些区域本身信噪比较低或局部对比度不足时，对该区域的恢复能力置信度进行衰减，避免将噪声较大的区域错误地当作可恢复区域。

通过上述步骤，可以得到当前图像对应的空间-语义恢复能力张量，用于指导后续 AE、AF 和 AWB 的联合决策。

#### 2. 基于恢复能力边界的拍摄参数可调范围确定

在本发明中，拍摄参数不再只根据当前图像统计量确定，而是基于图像退化特征和 ISP 恢复能力共同确定。

首先，根据第一图像的图像特征确定退化特征。退化特征可以包括由 AF 模块关联的模糊程度、由 AWB 模块关联的偏色程度，以及由 AE 模块关联的高光裁剪程度和噪声程度。

其次，根据设备温度、拍摄帧率、拍摄模式和语义区域确定 ISP 恢复能力。对于每一个语义区域，比较该区域的退化程度与对应恢复能力，得到差值信息。

进一步地，根据差值信息确定可靠性度量。当某一区域的退化小于 ISP 可恢复能力时，该区域可以作为可靠输入；当退化接近或超过 ISP 可恢复能力时，该区域对应的统计量或特征需要被降低权重，或者触发对前端拍摄参数的修正。

最后，根据各语义区域的可靠性度量确定拍摄参数的可调范围。拍摄参数可以包括 ISO、曝光时间、镜头位置或焦距、白平衡参数。该可调范围用于约束后续 AE、AF、AWB 的实际参数输出。

#### 3. 物理约束AE模块

物理约束 AE 模块用于在满足目标亮度的同时，避免曝光决策造成 AF 不可恢复的模糊和 AWB 不可恢复的颜色采样失效。

##### Innovation 2.1: 基于恢复能力边界的AE意图修正

该模块首先根据 RAW 图像、直方图、语义图和时序曝光状态，生成初始 AE 意图。初始 AE 意图可以包括初始曝光时间、初始 ISO 和初始 AE 特征。

然后，模块接收来自 AF、AWB 和 ISP 恢复能力边界的信息。其中，AF 可以提供运动模糊、失焦或对焦可靠性需求；AWB 可以提供高光裁剪、颜色采样有效性或色彩稳定性需求；ISP 恢复能力边界用于表示当前场景下噪声、模糊、高光和色彩退化的最大可恢复程度。

进一步地，AE 模块根据语义加权池化、主体感知池化、高光感知池化和可信色彩池化，分别生成噪声引导、运动模糊引导、高光引导和色彩引导。每一类引导信号均与对应可靠性分数相乘，形成可靠性引导特征。

最后，基于上述可靠性引导特征对初始 AE 意图进行修正，得到曝光时间和 ISO 的修正量。通过该方式，AE 不再只追求亮度目标，而是在 ISP 可恢复边界内选择不会永久破坏 AF 和 AWB 输入的曝光参数。

例如，在低光运动场景中，如果延长曝光时间会导致运动模糊超过 AF 可恢复范围，则 AE 将优先缩短曝光时间，并在 ISP 噪声恢复能力允许范围内提高 ISO。若高 ISO 噪声也超过 ISP 恢复能力，则 AE 会在亮度、噪声和对焦可靠性之间进行折中。

#### 4. 可靠性引导AF特征校正模块

可靠性引导 AF 特征校正模块用于解决 AE 和 AWB 对 AF 输入特征造成的污染问题。

##### Innovation 2.2: 基于跨3A退化信号的AF特征校正

该模块以左、右双像素 RAW 图像、镜头位置、照度、相位误差和时序 AF 状态作为输入，利用双像素骨干网络提取相位特征、纹理特征和几何特征。

同时，该模块接收来自 AE 的噪声退化和运动模糊退化，接收来自 AWB 的增益漂移退化，并接收 ISP 对失焦模糊的恢复能力边界。

在此基础上，模块构建可靠性约束编码器，将 AE 噪声、AE 运动模糊、AWB 增益漂移、ISP 失焦恢复能力和时间状态编码为可靠性约束特征。该可靠性约束特征用于判断当前双像素特征中的哪些成分可信，哪些成分可能由噪声、运动模糊或颜色增益变化引入。

进一步地，模块通过迭代校正块对 AF 特征进行局部残差修正。每个校正块基于可靠性约束对特征通道进行调制，使不可靠的相位、纹理或几何特征权重降低，使可靠特征得到增强。

最终，AF 模块输出焦点移动方向、焦点移动量和是否继续搜索的信息，并向 AE 反馈当前对焦过程中不可接受的模糊需求。由此，AF 不再被动接收被 AE 或 AWB 污染后的特征，而是在联合 3A 框架中主动修正对焦表示。

#### 5. 因果调制AWB模块

因果调制 AWB 模块用于解决过曝、噪声和失焦导致的颜色统计失效，以及视频中的白平衡帧间不稳定问题。

##### Innovation 2.3: 基于有效颜色证据池和时序置信度的AWB估计

该模块首先构建有效颜色证据池。具体地，根据 RAW 图像计算颜色统计信息，并结合 AE 产生的高光退化图和 ISP 色彩恢复能力，生成有效颜色掩码。对于已经饱和或预测在增益调整后会溢出的像素，AWB 不将其作为有效颜色样本。

如果预测的增益后溢出超过 ISP 可恢复边界，则 AWB 在拍摄前向 AE 发出硬约束需求，要求 AE 降低曝光或保护高光区域，而不是在图像已经过曝后再尝试后处理修正。

进一步地，AWB 模块采用空间约束条件提取稳定的光源颜色特征。对于失焦严重或低纹理区域，通过失焦可靠性编码器和空间光照注意力机制，动态调整不同区域的颜色统计权重。其目的在于从真实光源和稳定物体表面中提取颜色信息，避免由虚焦、噪声或局部反光造成错误白平衡。

再进一步地，AWB 模块利用时序颜色稳定机制评估当前帧决策置信度。当 AE 引入的噪声超过 ISP 最大恢复能力，或者当前帧有效颜色证据不足时，AWB 降低当前帧权重，并更多地参考历史帧白平衡状态。反之，当场景光照确实发生变化且当前帧证据可靠时，AWB 允许白平衡增益及时更新。

通过上述方式，AWB 能够输出稳定的光照估计结果，减少由 AE 高 ISO、AF 失焦或局部高光饱和引起的颜色闪烁，并避免 AWB 增益跳变对 AF 对比度特征造成干扰。

#### 6. 联合3A策略

本发明的联合 3A 策略可以理解为在 ISP 恢复能力约束下最小化总退化。

具体地，对于当前图像，系统首先估计各模块可能引入的退化，包括 AE 引起的噪声和运动模糊、AF 引起的失焦模糊、AWB 引起的颜色增益漂移，以及高光裁剪和色彩偏移。随后，系统将这些退化与 ISP 对应恢复能力进行比较。

若某一退化处于可恢复范围内，则该退化可以作为可接受的参数代价；若某一退化接近或超过 ISP 恢复能力边界，则系统通过联合策略调整对应 3A 参数，避免该退化成为不可恢复损伤。

该联合策略的核心不是让某一个模块绝对优先，而是根据场景内容和恢复能力动态分配退化预算。例如，人脸区域的颜色和纹理更重要，则 AWB 和降噪约束更严格；高速运动区域的曝光时间更敏感，则 AE 约束更严格；高光区域包含有效颜色样本时，则 AWB 会向 AE 发出保护高光的需求。

#### 7. 方法流程

本发明的方法可以包括以下步骤：

Step1：获取第一图像以及对应的图像特征、语义图、图像统计信息、相机参数和设备状态。

Step2：基于第一图像的图像特征确定退化特征。退化特征可以包括模糊程度、偏色程度、高光裁剪程度和噪声程度。

Step3：基于设备温度、拍摄帧率、拍摄模式、语义区域和图像统计信息，确定 ISP 对各语义区域的恢复能力。

Step4：对于各语义区域，比较退化特征和恢复能力，确定差值信息和可靠性度量。

Step5：基于可靠性度量确定拍摄参数的可调范围。拍摄参数包括 ISO、曝光时间、镜头位置或焦距、白平衡参数。

Step6：基于物理约束 AE 模块修正初始 ISO 和曝光时间，避免亮度目标造成不可恢复噪声、运动模糊或高光裁剪。

Step7：基于可靠性引导 AF 特征校正模块修正对焦特征，并确定镜头位置或焦距。

Step8：基于因果调制 AWB 模块过滤无效颜色样本，结合时序置信度确定白平衡参数。

Step9：输出联合确定的拍摄参数，并将各模块产生的退化反馈信号写入共享状态，用于后续帧或后续拍摄周期的联合控制。

#### b. 发明的效果

##### 1. 技术效果

本发明至少能够产生以下技术效果：

1. 提升低光运动场景下的对焦可靠性。由于 AE 在延长曝光和提高 ISO 时会受到 AF 可恢复模糊边界和 ISP 噪声恢复能力约束，因此可以避免为了亮度而造成不可恢复的运动模糊和相位检测失败。

2. 减少高对比夜景中的偏色。由于 AWB 能够在采样前识别过曝导致的无效颜色区域，并向 AE 发送高光保护需求，因此可以避免 AE 过曝破坏 AWB 颜色采样。

3. 提升混合光照视频中的白平衡稳定性。由于 AWB 根据当前帧有效颜色证据和历史帧信息自适应融合，因此能够在真实光照变化时及时响应，在噪声或失焦导致误判时保持稳定。

4. 减少前端 3A 与后端 ISP 的目标冲突。通过将 ISP 恢复能力边界前向注入 AE、AF、AWB 决策，本发明使前端拍摄参数不再产生超过后端能力的不可恢复退化。

5. 提升最终图像质量。相比仅基于亮度、对比度或颜色统计的传统 3A，本发明在拍摄前即考虑噪声、模糊、高光、偏色等退化是否可恢复，从而提高图像清晰度、颜色准确性和整体保真度。

##### 2. 使用场景

本发明可用于以下场景：

1. 手机低光拍摄场景，尤其是低光运动目标拍摄；

2. 夜景高动态范围拍摄场景，例如路灯、车灯、广告牌等高光源较多的场景；

3. 混合光照视频拍摄场景，例如室内外切换、舞台灯光、城市夜景视频；

4. 连续自动对焦和连续自动曝光视频场景；

5. 高帧率拍摄、HDR 拍摄、多帧降噪和计算摄影场景；

6. 需要前端相机控制和后端 ISP 算法联合优化的电子设备。

---

## 三、完成前一致性检查

### 1. 与联合3A方案内容的一致性检查

已覆盖图片材料中的主要技术内容：

- 已覆盖 AE、AF、AWB 独立反馈闭环的现有商业架构；
- 已覆盖低光运动、极端高对比夜景、混合光照视频三类失败场景；
- 已覆盖“共享状态只是被动仓库，缺少联合优化”的问题；
- 已覆盖“前端 3A 目标不考虑 ISP 最大恢复能力”的核心缺陷；
- 已覆盖 ISP 恢复能力预标定，包括噪声、失焦、运动模糊、高光和色彩预算；
- 已覆盖 Lux、Semantic、Temperature、FPS、Mode 五维能力空间；
- 已覆盖 ISP 空间-语义恢复能力调制模块；
- 已覆盖物理约束 AE、可靠性引导 AF、因果调制 AWB 三个核心模块；
- 已覆盖基于退化特征和恢复能力确定拍摄参数可调范围；
- 已覆盖 ISO、曝光时间、镜头位置或焦距、白平衡参数的联合输出；
- 已覆盖跨模块退化反馈，包括 AE 噪声、AE 运动模糊、AF 失焦、AWB 偏色或高光裁剪需求。

### 2. 与参考DOI文档风格的一致性检查

本文档已按照参考 DOI 的写作结构和表达方式处理：

- 使用“发明背景介绍 / 发明的详细说明 / 发明的效果 / 使用场景”的交底结构；
- 采用“现有技术说明 → 现有问题 → 本发明目的 → 发明构成 → Innovation 分解”的逻辑；
- 使用“具体地”“进一步地”“基于”“通过”“实现”等专利交底常用表达；
- 保留类似参考 DOI 的 Innovation 编号方式；
- 重点描述技术模块、数据流、控制逻辑和技术效果，而不是写成论文摘要或营销说明；
- 将每个技术效果与对应技术手段进行关联说明。

结论：当前 DOI 内容与联合 3A 方案一致，写作结构和语言风格与参考 DOI 文档一致，可作为后续正式专利撰写的发明交底基础。
